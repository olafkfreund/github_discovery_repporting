// OpenAICompatFields — no extra config fields required.
// The top-level endpoint_url field (always visible for this provider) is sufficient.

export default function OpenAICompatFields() {
  return <></>
}
