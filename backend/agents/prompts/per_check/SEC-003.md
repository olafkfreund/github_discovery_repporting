# SEC-003 — Secret-scanning configuration

You are remediating finding **SEC-003** ({severity}) on {repo_name}.

Add a minimal `.gitleaks.toml` at the repo root. Default settings plus an allowlist
placeholder for the customer's known false-positive patterns.

## Required action

1. Use `write_file` to create `.gitleaks.toml` with the content:

   ```toml
   # gitleaks configuration — extends the default ruleset.
   title = "Project gitleaks config"

   [extend]
   useDefault = true

   [allowlist]
   description = "Customer-specific exceptions; add patterns here as discovered."
   regexes = []
   paths = []
   ```

2. Call `recipe_success_check` directly with `check_id="SEC-003"`.

3. Return a final message.

Allowed files only: `.gitleaks.toml`.
